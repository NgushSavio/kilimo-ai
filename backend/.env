# Server
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# PostgreSQL (works with Supabase, Render, Railway, or local Postgres)
# Use the POOLER/transaction connection string if your host is Supabase,
# not the direct db host, to avoid DNS/connection issues.
DATABASE_URL="postgresql://postgres.mlfdmelvssnhcbbhicpz:%40R_1dRESETPASSWORD@aws-0-eu-west-1.pooler.supabase.com:5432/postgres"

# Anthropic Claude API
ANTHROPIC_API_KEY="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"
CLAUDE_MODEL=claude-sonnet-5

# Firebase Admin (service account, used to verify Admin login tokens)
# Paste the full JSON from Firebase Console > Project Settings > Service Accounts
# as a single-line string, or point to a file path instead.
# Server

# Firebase Admin (Single string JSON so your Node code reads it cleanly)
FIREBASE_SERVICE_ACCOUNT_PATH="./firebase-service-account.json"

DEFAULT_RATE_PER_KM=65

# Transport cost estimate (KES per km per bag-load), used until real
# transporter data is wired in
DEFAULT_RATE_PER_KM=65
